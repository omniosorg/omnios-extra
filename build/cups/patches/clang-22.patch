diff -wpruN --no-dereference '--exclude=*.orig' a~/scheduler/process.c a/scheduler/process.c
--- a~/scheduler/process.c	1970-01-01 00:00:00
+++ a/scheduler/process.c	1970-01-01 00:00:00
@@ -639,7 +639,7 @@ cupsdStartProcess(
 
   cupsdLogMessage(CUPSD_LOG_DEBUG2, "cupsdStartProcess: Calling posix_spawn.");
 
-  if (posix_spawn(pid, exec_path, &actions, &attrs, argv, envp ? envp : environ))
+  if (posix_spawn((pid_t *)pid, exec_path, &actions, &attrs, argv, envp ? envp : environ))
   {
     cupsdLogMessage(CUPSD_LOG_ERROR, "Unable to fork %s - %s.", command, strerror(errno));
 
